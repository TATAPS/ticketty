const { login, register, logout } = require("../models/auth.js");
// import bcrypt from "bcryptjs";
// import jwt from "jsonwebtoken";

export const register = (req, res) => {
};

export const login = (req, res) => {

};

export const logout = (req, res) => {
}
